apiVersion: cost.k8smgmt.io/v3
kind: CostProfile
metadata:
  name: test
  project: giri
spec:
  installationParams:
    azure:
      customPricing:
        cloudCredentialsName: giri-aks
  providerType: azure
  version: v1
